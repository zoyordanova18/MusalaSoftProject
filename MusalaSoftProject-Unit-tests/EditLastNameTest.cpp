#include "pch.h"
#include "CppUnitTest.h"
#include "../MusalaSoftProject/students.h"
#include "HelperFunctionsForTesting.h"

using namespace Microsoft::VisualStudio::CppUnitTestFramework;

namespace MusalaSoftProjectUnitTests
{
	TEST_CLASS(editLastNameTest)
	{
	public:

		TEST_METHOD(shouldEditLastName)
		{
			/*// Arrange
			createFilesNeededForTesting("students_editLastNameTest.txt");

			// Act
			STUDENT_SERVICE::editLastName(3, "Marinova");
			std::vector<STUDENT> students = STUDENT_SERVICE::getAll();
			STUDENT student = findStudentById(students, 3);
			STUDENT_SERVICE::close();

			// Assert
			Assert::AreEqual(student.lastName, "Marinova");*/
		}
	};
}
